<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Define the background properties like color etc -->
    <item android:id="@android:id/background"
        android:top="2dp"
        android:bottom="2dp"
        android:left="2dp"
        android:right="2dp">
        <shape>
            <corners android:radius="12dip"/>
            <gradient
                android:startColor="#2F2F31"
                android:centerColor="#2F2F31"
                android:centerY="1.0"
                android:endColor="#2F2F31"
                android:angle="270"
                />
        </shape>
    </item>

    <!-- Define the progress properties like start color, end color etc -->
    <item android:id="@android:id/progress"
        android:top="2dp"
        android:bottom="2dp"
        android:left="2dp"
        android:right="2dp">
        <clip>
            <shape>
                <corners android:radius="12dip"/>
                <gradient
                    android:startColor="#FF62A5"
                    android:centerY="1.0"
                    android:endColor="#FF8960"
                    android:angle="270"
                    />
            </shape>
        </clip>
    </item>

</layer-list>